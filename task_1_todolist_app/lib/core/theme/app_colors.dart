import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color background = Color(0xFFEAFBF4);
  static const Color card = Color(0xFFE1F4EC);

  // Primary brand
  static const Color primary = Color(0xFF0B4F3C);

  // Text
  static const Color textPrimary = Color(0xFF063D2E);
  static const Color textMuted = Color(0xFF4F7F6B);

  // Icons
  static const Color iconPrimary = Color(0xFF0B4F3C);
  static const Color iconMuted = Color(0xFF7AAE99);
}
